[allowlist]
  description = "Global Allowlist"

  paths = [
    # Generated MkDocs HTML reports listing Kubernetes Secret resource names
    # and types (no actual credentials). Covers both current and legacy layouts.
    '''security/index\.html$''',
    '''secrets-inventory/index\.html$''',
  ]
